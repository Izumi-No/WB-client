import React from "react"
import { ThemeProvider, ColorModeProvider, ColorModeScript} from "@chakra-ui/react"
import {theme} from '@chakra-ui/react'

export default function ChakraProvider({ children }){ 
    return(    


)}